#include <iostream>
#include <functional>
#include "algebra/math.h"
/*user header to compile*/

#ifdef _WIN32 || _WIN64
	#pragma warning (disable:4996)
#endif

#pragma once

using namespace std;
int main(int argc, char * argv[])
{

	
	return 0;
}